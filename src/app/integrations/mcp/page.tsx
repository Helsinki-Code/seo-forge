import type { Metadata } from "next";
import { Braces, KeyRound, ShieldX, TerminalSquare } from "lucide-react";
import { PublicCard, PublicChecklist, PublicCta, PublicFaq, PublicHeading, PublicHero, PublicSection, PublicShell, PublicSteps } from "@/components/marketing/PublicPrimitives";
export const metadata:Metadata={title:"Remote MCP integration — SEOForge",description:"Site-bound read-only GA4 MCP access with revocable scoped tokens and no production tools.",alternates:{canonical:"/integrations/mcp"}};
export default function McpIntegration(){return <PublicShell><PublicHero eyebrow="Live integration · limited scope" title="Give an approved AI client read-only analytics—not production authority." description="The current remote MCP surface is site-bound GA4 reporting. Its bearer token can list property context and run authorized Analytics reports; approval, publishing, credentials and branch controls are deliberately absent." primary="Choose Growth" primaryHref="/pricing" secondary="Review Developer Workflows" secondaryHref="/developers" visual={<div className="marketing-card p-7 font-mono text-xs"><p className="text-mint">scope: ga4:read</p><p className="mt-3">tool: run_report</p><p className="mt-2">site: sf_site_•••</p><p className="mt-3 text-rose">publish tools: none</p></div>}/><PublicSection><div className="grid gap-5 md:grid-cols-3"><PublicCard icon={Braces} title="Exact capability" body="Initialize an MCP session, list the GA4 reporting tools exposed by SEOForge and run reports against the site’s selected active GA4 connection."/><PublicCard icon={KeyRound} tone="amber" title="Requested access" body="A separately issued SEOForge MCP bearer token with the single ga4:read scope; browser clients must also satisfy allowed-origin policy."/><PublicCard icon={ShieldX} tone="rose" title="Cannot" body="Approve, merge, deploy, publish, retrieve credentials, bypass protection, switch to another site or access general SEOForge operations not exposed by the current GA4 server."/></div></PublicSection><PublicSection muted><div className="grid gap-10 lg:grid-cols-2"><div><PublicHeading eyebrow="Token protection" title="Opaque, hashed and revocable."/><PublicChecklist items={["Raw token is shown only when created","Only a keyed hash is stored","Token binds to one site and explicit scope","Provider OAuth grant never reaches the MCP client","Revocation invalidates the token immediately"]}/></div><div><PublicHeading eyebrow="Setup" title="Connect GA4 before issuing a client token."/><PublicSteps steps={["Choose Growth or Agency capacity","Connect and select the site’s GA4 property","Create a labeled MCP token in settings","Configure the remote endpoint and bearer token","Run a bounded report and verify site context"]}/></div></div></PublicSection><PublicSection><PublicHeading eyebrow="Example workflow" title="An approved assistant compares landing-page observations." body="The client calls run_report for organic sessions and key events across two declared date ranges. SEOForge executes against the site-bound GA4 grant and returns the provider result; the client receives no Google refresh token and cannot request a site change."/><PublicCard icon={TerminalSquare} tone="mint" title="Limitations" body="This is not a general-purpose production MCP and currently exposes GA4 reporting only. GA4 data-quality, privacy and attribution constraints still apply. Token rotation and client-side secret storage remain the customer’s responsibility."/></PublicSection><PublicFaq items={[["Can the MCP client approve an SEO proposal?","No. Production approval is intentionally unavailable through MCP."],["Does the MCP token replace Google OAuth?","No. The site must already have an active customer-owned GA4 connection."]]}/><PublicCta title="Use AI clients for scoped analysis, not hidden deployment." body="Choose Growth or Agency, connect GA4 and issue only the site-bound token the client needs."/></PublicShell>}
